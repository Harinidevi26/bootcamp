// app/api/verify-razorpay-payment/route.ts — placeholder
import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ verified: false, message: "Razorpay payment verification placeholder" });
}
