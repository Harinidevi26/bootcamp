// app/api/create-razorpay-order/route.ts — placeholder
import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ orderId: null, message: "Razorpay order creation placeholder" });
}
